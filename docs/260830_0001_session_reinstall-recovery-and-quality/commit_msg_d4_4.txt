refactor(repo): remove root mcp-servers copy per D4 merge decision

With the dual mcp-servers merge complete (source of truth =
extension/mcp-servers), the root copy was a byte-identical duplicate
(114000 report: mirror 67/67 diff=0) that would drift the next time
someone edited the wrong tree.

What: delete root mcp-servers/ (59 tracked files) plus strayed root
start_vibezoo_bridge.bat / start_vibezoo_servers.bat via Recycle Bin
Why: user-approved D-4 Option A (decisions.md) - single canonical
source eliminates double-maintenance; all config/scripts/import paths
already referenced extension/mcp-servers
Impact: no functional change; canonical bridge lives in
extension/mcp-servers and ships in the VSIX; docs updated separately
in the docs phase (41 stale references catalogued in 114500 report)

Verification: mirror compare after removal shows only_ext only
(expected), only_mir=0; py_compile and pytest unaffected (extension
paths unchanged)

Refs: docs/260830_0001_session_reinstall-recovery-and-quality/114500_code-light-d4-4-removal-report.md