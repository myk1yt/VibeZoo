feat(image): rebuild Dropzone for one-paste image handoff to AI

Users had an image paste feature but rarely used it because the flow
was cumbersome: save, find the path, assemble markdown by hand.

What: Dropzone webview now accepts Ctrl+V and drag-drop images, saves
them instantly with thumbnail + resolution/size/format chips; copies
![file](path) markdown to the clipboard automatically
(vibezoo.image.autoAnalyze, default on); keeps last 10 images with
click/Enter re-copy; toolbar for md-copy/path-copy/open-in-VS
Code/clear; dark-mode theme variables and keyboard access throughout;
new l10n strings deployed across all 20 languages, nls config string
in 20 languages
Why: REQ-007 - make paste-to-AI a two-step flow (copy image, Ctrl+V)
with guaranteed minimum utility even when the vision model is absent
Impact: no API changes; VisualVibePanels Dropzone HTML/JS revised;
compiles clean

Verification: npm run compile (tsc -p .) 0 errors; nls/l10n key sync
20/20 languages

Refs: docs/260830_0001_session_reinstall-recovery-and-quality/104500_code-d3-1-dropzone-report.md