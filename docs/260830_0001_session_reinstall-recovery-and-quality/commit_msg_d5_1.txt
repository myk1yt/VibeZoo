chore(bridge): align root config.py version with canonical source

Root mcp-servers/bridge/config.py still reported VERSION 0.14.4 while
the canonical extension copy and package.json are 0.15.1, which leaked a
stale version into health check payloads, the startup banner, fix loop
responses and setup output.

What: bump mcp-servers/bridge/config.py VERSION to 0.15.1
Why: D5-1 version consistency - bridge-reported version must match the
shipped extension until D4 removes the root copy entirely
Impact: display-only; py_compile OK both copies

Verification: py_compile root/extension OK; VERSION loads 0.15.1 both

Refs: docs/260830_0001_session_reinstall-recovery-and-quality/105500_code-light-d5-1-version-report.md